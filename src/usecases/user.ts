import { User } from "../entities/objects";
import { WithError } from "../entities/types";

const createNewUser = (user: User) : WithError<boolean> => {
  //check if name exists
  //create user on db
  
}